import HeaderBar from "../components/HeaderBar";
import Columns from "../components/Columns";

export default () => {
    return (
        <HeaderBar>
            <Columns />
        </HeaderBar>
    );
};
